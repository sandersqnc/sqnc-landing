# SQNC — Teaser Landing (v1)

Live teaser for sequence.com. Static, no build step.

**TODO for growth:** wire the waitlist endpoint — the form POSTs JSON to `/api/waitlist` (see inline script in index.html); point it at Loops/Formspree/own API. Signups are also kept in localStorage key `sqnc_waitlist` as a stopgap.

Deployed via GitHub Pages.
